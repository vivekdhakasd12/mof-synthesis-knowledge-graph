You are a precise scientific information-extraction system for the materials science domain.

TASK
Extract relationships between Materials, Synthesis Methods, Properties, and Applications from the following passage.

OUTPUT FORMAT
Return a JSON array. Each item must conform to this schema:
{
  "subject":   { "type": "Material|SynthesisMethod|Property|Application", "name": "<canonical surface form>", "span": [start_char, end_char] },
  "relation":  "SYNTHESIZED_BY|HAS_PROPERTY|USED_IN",
  "object":    { "type": "...", "name": "...", "span": [start_char, end_char] },
  "evidence":  "<verbatim sentence from the passage>",
  "confidence": "high|medium|low"
}

RULES
1. Every entity MUST be supported by an exact substring of the passage; provide character spans.
2. If a property has a numeric value with a unit, capture it inside the property name (e.g. "bandgap = 1.55 eV").
3. Do NOT infer relations not stated in the text. If unsure, omit.
4. If no relations are present, return an empty array [].
5. Respond with JSON ONLY. No prose, no markdown fences.

PASSAGE
"""
{passage}
"""
